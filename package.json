{
  "name": "resend-invoice-demo",
  "version": "1.0.0",
  "description": "Simple invoice email service using Resend — generates PDF invoices and sends them with optional scheduled receipt emails.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pdfkit": "^0.17.2",
    "resend": "^6.9.2",
    "svix": "^1.85.0"
  }
}
